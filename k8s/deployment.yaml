apiVersion: apps/v1
kind: Deployment
metadata:
  name: catatan-sederhana
spec:
  replicas: 1
  selector:
    matchLabels:
      app: catatan-sederhana
  template:
    metadata:
      labels:
        app: catatan-sederhana
    spec:
      containers:
        - name: laravel
          image: username_dockerhub/catatan-sederhana:latest
          ports:
            - containerPort: 8000
          command: ["php"]
          args:
            ["artisan", "serve", "--host=0.0.0.0", "--port=8000"]
          env:
          - name: APP_KEY
            value: "base64:12pBkfT/k4qamfpFGovJpYyHuOKkjZ20p8UL2x8aHD0="
          - name: APP_ENV
            value: "production"
          - name: APP_DEBUG
            value: "false"
